# THÀNH VIÊN 3/6: Cài đặt logic bên trong các hàm này.

def fleury_algorithm(nodes, edges):
    """
    Tìm Đường đi hoặc Chu trình Euler sử dụng thuật toán Fleury.
    
    Tham số:
        nodes (list): Danh sách các đối tượng Node.
        edges (list): Danh sách các đối tượng Edge.
        
    Trả về:
        list: Danh sách các ID đỉnh biểu diễn đường đi/chu trình Euler.
        Trả về None nếu không tồn tại đường đi như vậy.
    """
    pass

def hierholzer_algorithm(nodes, edges):
    """
    Tìm Chu trình Euler sử dụng thuật toán Hierholzer.
    
    Tham số:
        nodes (list): Danh sách các đối tượng Node.
        edges (list): Danh sách các đối tượng Edge.
        
    Trả về:
        list: Danh sách các ID đỉnh biểu diễn chu trình Euler.
        Trả về None nếu không tồn tại chu trình như vậy.
    """
    pass
